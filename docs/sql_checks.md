## Неделя 5: Загрузка витрины в SQLite

### Параметры
- **Таблица:** mart_world_bank
- **База данных:** data/mart.db
- **Количество строк:** 66
- **Диапазон лет:** 1960-2025

---

## Проверка 1: Таблица не пустая

**SQL-запрос:**
```sql
SELECT COUNT(*) FROM mart_world_bank;

## Проверка 2: Диапазон лет
SQL-запрос:

SELECT MIN(year), MAX(year) FROM mart_world_bank;

## Проверка 3: NULL в колонке value
SQL-запрос:

SELECT COUNT(*) FROM mart_world_bank WHERE value IS NULL;


## Проверка 4: Дубликаты по году
SQL-запрос:

SELECT year, COUNT(*) 
FROM mart_world_bank 
GROUP BY year 
HAVING COUNT(*) > 1;

## Проверка 5: Статистика по ВВП
SQL-запрос:

SELECT 
    ROUND(AVG(value), 2) as avg_gdp,
    MIN(value) as min_gdp,
    MAX(value) as max_gdp
FROM mart_world_bank 
WHERE value IS NOT NULL;
```
```
1. ПРОВЕРКА: Таблица не пустая
----------------------------------------
   Количество строк: 66
     ПРОВЕРКА ПРОЙДЕНА

2. ПРОВЕРКА: Диапазон лет
----------------------------------------
   Годы: 1960 - 2025
   ПРОВЕРКА ПРОЙДЕНА

3. ПРОВЕРКА: NULL в колонке value
----------------------------------------
   NULL в value: 1
   ПРОВЕРКА ПРОЙДЕНА (только 2025 год)

4. ПРОВЕРКА: Дубликаты по году
----------------------------------------
   Дубликатов: 0
   ПРОВЕРКА ПРОЙДЕНА

5. ПРОВЕРКА: Статистика по ВВП
----------------------------------------
   Средний ВВП: $23,629.11
   Минимальный ВВП: $1,162.12
   Максимальный ВВП: $56,103.73
   ```
